{
  "name": "compute-orchestrator",
  "version": "0.1.0",
  "private": true,
  "description": "Compute Network orchestrator — node registry, pipeline scheduling, OpenAI-compatible API",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@hono/node-server": "^1.14.0",
    "@supabase/supabase-js": "^2.49.0",
    "dotenv": "^17.3.1",
    "hono": "^4.7.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
