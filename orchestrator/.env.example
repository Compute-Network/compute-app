SUPABASE_URL=https://eqkdwakvhwzsdmnklqfs.supabase.co
SUPABASE_SERVICE_KEY=your-service-role-key-here
PORT=3000

# Solana (devnet) — optional, defaults to deploy-keypair.json
# SOLANA_KEYPAIR=[1,2,3,...] — JSON array of secret key bytes
# SOLANA_RPC_URL=https://api.devnet.solana.com
# COMPUTE_MINT=<mint-address> — set after first run to reuse existing mint
